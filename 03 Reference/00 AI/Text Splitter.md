#chunk-strategy
**Best use case:**  
1. Chunking large documents into token-aware segments for embeddings/RAG - preserves context while optimizing retrieval accuracy and LLM cost.

**Alternative:** 
1. [[LlamaIndex]] when you want higher-level indexing (semantic chunking, metadata-aware splits) with less manual tuning
